<%- include('../partials/navabar') %>
<div class="container-fluid">
  <div class="row">
    <div class="col-2 bg-light" style="height: 100vh">
      <nav class="nav flex-column" style="height: 100%">
        <!-- <% if (user.role === "admin") { %>
        <h2 class="text-center">ضباط وضباط الصف والجنود</h2>
        <% } else if (user.role === "shon2_dobat") { %>
        <h2 class="text-center">ضباط</h2>
        <% } else if(user.role === "katebah") { %>
        <h2 class="text-center">ضباط الصف والجنود</h2>
        <%}%> -->
        <h2 class="text-center pt-5">لوحه تحكم</h2>
        <!-- <i class="bi bi-house-door-fill"></i> -->
        <a class="nav-link text-dark mt-5" href="/"
          ><i class="bi bi-house-door-fill"></i> الصفحه الرئيسيه</a
        >
        <a class="nav-link text-dark" href="/new_person"
          ><i class="bi bi-person-fill-add"></i> إضافة فرد جديد</a
        >
      </nav>
    </div>

    <% if(user.role === "admin"){%> <%- include("./admin") %> <% } else
    if(user.role === "shon2_dobat"){ %> <%- include("./shon2_dobat") %> <% }
    else if (user.role === "katebah") { %> <%- include("./katebah") %> <% } %>
  </div>
</div>

<%- include('../partials/footer') %>
